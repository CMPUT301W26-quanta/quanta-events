<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ScrollView
        android:id="@+id/scroll_view"
        android:layout_width="0dp"
        android:layout_marginTop="2dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/action_row"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:paddingTop="8dp"
            android:paddingBottom="12dp">

            <CheckBox
                android:id="@+id/check_entrant"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:checked="true"
                android:text="Make me an entrant."
                android:textColor="@color/text_color"
                app:buttonTint="@color/color_dark_purple"
                app:strokeColor="@color/border"
                app:strokeWidth="2dp" />

            <CheckBox
                android:id="@+id/check_organizer"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:checked="true"
                android:text="Make me an organizer."
                android:textColor="@color/text_color"
                app:buttonTint="@color/color_dark_purple"
                app:strokeColor="@color/border"
                app:strokeWidth="2dp" />

            <CheckBox
                android:id="@+id/check_admin"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Make me an administrator."
                android:textColor="@color/text_color"
                app:buttonTint="@color/color_dark_purple"
                app:strokeColor="@color/border"
                app:strokeWidth="2dp" />
        </LinearLayout>
    </ScrollView>

    <LinearLayout
        android:id="@+id/action_row"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <Button
            android:id="@+id/save_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:layout_marginStart="16dp"
            android:backgroundTint="@color/color_dark_purple"
            android:text="Save"
            android:textColor="@color/text_color"
            app:cornerRadius="12dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:strokeColor="@color/border"
            app:strokeWidth="2dp"
            android:layout_weight="1" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
